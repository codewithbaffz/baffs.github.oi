{
  "version": 2,
  "builds": [
    {
      "src": "schedulfy/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "schedulfy/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
